{
  "name": "@mfs-tools/cli",
  "description": "CLI tools for the project.",
  "version": "1.0.0",
  "bin": {
    "mfs-clean": "./dist/clean/index.js",
    "mfs-graph": "./dist/graph/index.js"
  },
  "scripts": {
    "build": "tsc && pnpm i --ignore-scripts",
    "build:watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^20.8.4",
    "dependency-cruiser": "^14.0.0",
    "typescript": "^5.2.2"
  }
}
